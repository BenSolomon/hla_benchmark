---
title: "1) Genotyping Accuracy"
output: html_notebook
---

# Setup 
```{r, message=F}
library(tidyverse)
library(ggh4x)
source("data_import_functions.R")
isb_path <- "/labs/khatrilab/solomonb/covid/isb"
isb_samples <- read_tsv("/labs/khatrilab/solomonb/covid/isb/logs/210217_232725/parallel.log") %>% 
  separate(Command, into = c(NA, "sample"), sep = " ") %>% 
  pull(sample) %>% unique()
```

```{r}
all_hla <- format_hla_table(combine_HLA_import(path = isb_path, samples = isb_samples)) %>% filter(sample %in% isb_samples)

# all_samples <- read_tsv(sprintf("%s/all_samples.txt", isb_path), col_names = "sample") %>% pull(sample)
# typed_hla <- all_hla %>% 
#   filter(grepl("^[ABCD]", locus), sample %in% all_samples)
```
# Prediction frequency

```{r}
prediction_frequency <- all_hla %>% 
  select(-allele_id) %>% 
  pivot_longer(cols = field_1:field_3, names_to = "field", values_to = "allele") %>% 
  pivot_wider(names_from = "genotyper", values_from = "allele", values_fn = function(x) sum(!is.na(x)), values_fill = 0) %>% 
  # filter(invitro != 0) %>% 
  ### NEED TO DEAL WITH HLAMINER AMBIGOUS ALLELES
  # mutate_at(vars(arcasHLA:hlaminer), funs(./invitro)) %>% 
  mutate_at(vars(arcasHLA:hlaminer), funs(./2)) %>% 
  select(-invitro) %>% 
  ungroup() %>% 
  pivot_longer(cols = arcasHLA:hlaminer, names_to = "genotyper", values_to = "frequency")

```

```{r, fig.width = 12, fig.height = 4}
prediction_frequency %>% 
  filter(grepl("^[ABC]", locus)) %>% 
  gg_frequency()
```

```{r, fig.width = 18, fig.height = 4}
dp <- prediction_frequency %>% 
  filter(grepl("^DP", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_frequency()

dq <- prediction_frequency %>% 
  filter(grepl("^DQ", locus) & genotyper %in% c("arcasHLA", "hlaminer", "phlat"),) %>% 
  gg_frequency()

dra <- prediction_frequency %>% 
  filter(grepl("^DRA", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_frequency()

drb1 <- prediction_frequency %>% 
  filter(grepl("^DRB1", locus) & genotyper %in% c("arcasHLA", "hlaminer", "phlat"),) %>% 
  gg_frequency()

drb345 <- prediction_frequency %>% 
  filter(grepl("^DRB[345]", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_frequency()

dp|
  strip_y(dq)|
  strip_y(dra)|
  strip_y(drb1)|
  strip_y(drb345)|
  plot_layout(widths = c(4,6,2,3,6))
```

# Accuracy

```{r}
hla_compare_woNA <- compare_hla(hla_df = all_hla, reference = "invitro", exclude_missing = F, compare_function = "accuracy")
```

```{r}
p1 <- hla_compare_woNA %>% 
  filter(grepl("^[ABC]", locus)) %>% 
  gg_accuracy() +
  ggtitle("MHC1 loci accuracy", subtitle = "Missing predictions excluded")
p1
```

# Updating molecular HLA typing across timepoints

```{r}
### Every subject has molecular HLA typing, but may occur in the BL, AC, or CV timepoint
### This expands the molecular HLA typing from the timepoint it occurs to all other time points 
### This allows for scRNA genotyping from a given subject and timepoint to be assessed even if that
### subjects molecular typing happened at a different time point. Since HLA is genetic, the molecular
### typing should not change

all_hla_expanded <- all_hla %>% 
  group_by(sample, genotyper) %>% 
  nest() %>% 
  filter(genotyper == "invitro") %>% 
  separate(sample, into = c("sample",NA), sep = "-") %>% 
  right_join(all_hla %>% 
               select(sample) %>% 
               distinct() %>% 
               separate(sample, into = c("sample","time"), sep = "-"),
             by = "sample") %>% 
  unnest(data) %>% 
  unite(sample, sample, time, sep = "-") %>% 
  bind_rows(all_hla %>% filter(genotyper != "invitro"))
```

```{r}
p2 <- compare_hla(hla_df = all_hla_expanded %>% filter(grepl("BL$", sample)), reference = "invitro", exclude_missing = F, compare_function = "accuracy") %>% 
  filter(grepl("^[ABC]", locus)) %>% 
  gg_accuracy() +
  ggtitle("MHC1 loci accuracy", subtitle = "Missing predictions excluded")
p2
```

```{r, fig.height = 6, fig.width = 12}
library(patchwork)
p1/p2
```

# Assemble accuracy

### Success
```{r}
success_df <- compare_hla(hla_df = all_hla_expanded %>% filter(grepl("BL$", sample)), 
            reference = "invitro", 
            exclude_missing = T, 
            compare_function = "accuracy") 
```
```{r, fig.width = 12, fig.height = 4}
success_df%>% 
  filter(grepl("^[ABC]", locus)) %>% 
  gg_accuracy()
```


```{r, fig.width = 16, fig.height = 4}
dp_s <- success_df %>% 
  filter(grepl("^DP", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_accuracy()

dq_s <- success_df %>% 
  filter(grepl("^DQ", locus) & genotyper %in% c("arcasHLA", "hlaminer", "phlat"),) %>% 
  gg_accuracy()

# dra_a <- success_df %>% 
#   filter(grepl("^DRA", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
#   gg_accuracy()

drb1_s <- success_df %>% 
  filter(grepl("^DRB1", locus) & genotyper %in% c("arcasHLA", "hlaminer", "phlat"),) %>% 
  gg_accuracy()

drb345_s <- success_df %>% 
  filter(grepl("^DRB[345]", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_accuracy()

dp_s|
  strip_y(dq_s)|
  # strip_y(dra_s)|
  strip_y(drb1_s)|
  strip_y(drb345_s)|
  plot_layout(widths = c(4,6,3,6))
```

### Accuracy

```{r}
accuracy_df <- compare_hla(hla_df = all_hla_expanded %>% filter(grepl("BL$", sample)), 
            reference = "invitro", 
            exclude_missing = F, 
            compare_function = "accuracy") 
```
```{r, fig.width = 12, fig.height = 4}
accuracy_df%>% 
  filter(grepl("^[ABC]", locus)) %>% 
  gg_accuracy()
```

```{r, fig.width = 16, fig.height = 4}
dp_a <- accuracy_df %>% 
  filter(grepl("^DP", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_accuracy()

dq_a <- accuracy_df %>% 
  filter(grepl("^DQ", locus) & genotyper %in% c("arcasHLA", "hlaminer", "phlat"),) %>% 
  gg_accuracy()

# dra_a <- accuracy_df %>% 
#   filter(grepl("^DRA", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
#   gg_accuracy()

drb1_a <- accuracy_df %>% 
  filter(grepl("^DRB1", locus) & genotyper %in% c("arcasHLA", "hlaminer", "phlat"),) %>% 
  gg_accuracy()

drb345_a <- accuracy_df %>% 
  filter(grepl("^DRB[345]", locus) & genotyper %in% c("arcasHLA", "hlaminer"),) %>% 
  gg_accuracy()

dp_a|
  strip_y(dq_a)|
  # strip_y(dra_a)|
  strip_y(drb1_a)|
  strip_y(drb345_a)|
  plot_layout(widths = c(4,6,3,6))
```
